plugins {
    id 'io.nextflow.nextflow-plugin' version '1.0.0-beta.10'
}

version = '0.1.0'

nextflowPlugin {
    nextflowVersion = '24.10.0'

    provider = 'nextflow'
    className = 'nextflow.slack.SlackPlugin'
    extensionPoints = [
        'nextflow.slack.SlackExtension',
        'nextflow.slack.SlackFactory'
    ]

}
